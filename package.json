{
  "name": "rawproto-wasm",
  "version": "1.0.3",
  "main": "pkg/rawproto.js",
  "types": "pkg/rawproto.d.ts",
  "description": "Decode protobuf in raw form, in wasm (for node, deno, cf workers, etc.) Based on decode_raw.",
  "scripts": {
    "build": "npx -y wasm-pack build --target web --out-dir web && wasm-pack build --target nodejs && cp src/index.html web/ && rm pkg/package.json web/package.json",
    "start": "npx -y live-server web",
    "test": "npx -y jest --verbose=true",
    "prepublish": "npm run build"
  },
  "sideEffects": false,
  "files": [
    "pkg/rawproto_bg.wasm",
    "pkg/rawproto.js",
    "pkg/rawproto.d.ts",
    "web/rawproto_bg.wasm",
    "web/rawproto.js",
    "web/rawproto.d.ts"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/konsumer/rawproto-wasm.git"
  }
}
